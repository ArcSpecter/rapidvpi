#include "vip_uart/common/uart_params.hpp"

namespace vip::uart {
// UartParams is intentionally header-only. This translation unit keeps the
// common CMake object target concrete and consistent with other VIP packages.
}
